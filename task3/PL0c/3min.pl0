const n = 5;
var min, i, current;

begin
  read(current);
  min := current;
  i := 1;
  while i < n do
    begin
      read(current);
      if current < min then
        min := current;
      i := i + 1
    end;
  write(min)
end.
